
{% if auth.check %}
<div class="header-block header-block-buttons">
    <a href="#" class="btn btn-sm header-btn" data-toggle="modal" data-target="#aclpage-modal">
        <i class="fa fa-users"></i>
        <span>Permissions</span>
    </a>
</div>
{% else %}


{% endif %}